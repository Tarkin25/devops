language: java

jobs:
  include:
    - stage: CodeQuality
      script: ./gradlew sonarqube
    - stage: HelloWorld
      script: ./gradlew helloWorld

addons:
  sonarcloud:
    organization: $SONAR_USER
    token:
      secure: $SONAR_TOKEN

jdk:
  - openjdk11
